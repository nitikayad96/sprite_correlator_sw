from setuptools import setup

__version__ = '0.1'

setup(name = 'ami',
      version = __version__,
      description = 'Control software / scripts for the ami digital correlator',
      url = "https://github.com/jack-h/ami_correlator_sw",
      requires    = ['numpy', 'corr', 'termcolor'],
      provides    = ['ami'],
      package_dir = {'ami':'src'},
      packages    = ['ami'],
)

